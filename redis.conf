# redis.conf
# Minimal settings for the co-located Redis instance.

bind 127.0.0.1
port 6379
daemonize yes
supervised no
protected-mode yes

# Disable persistence – data in the free tier is ephemeral anyway.
save ""
appendonly no
